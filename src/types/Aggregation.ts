export enum FREQUENCY {
  YEAR = "year",
  QUARTER = "quarter",
  MONTH = "month",
  WEEK = "week",
  DAY = "day",
}

export enum NUMBER_AGGREGATION {
  COUNT = "count",
  MEAN = "mean",
  MIN = "min",
  MAX = "max",
  SUM = "sum",
}
